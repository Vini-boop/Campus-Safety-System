/**
 * empty-stub.js
 *
 * A completely empty module used by the Metro web resolver to stub out
 * native-only React Native internals (e.g. codegenNativeCommands) that
 * react-native-maps imports but that do not exist on the web platform.
 */
module.exports = {};
